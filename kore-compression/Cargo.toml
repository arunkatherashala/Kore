[package]
name = "kore-compression"
version = "0.1.0"
edition = "2021"

[dependencies]
zstd = "0.13"
brotli = "1.0"
sha2 = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
rand = "0.8"
