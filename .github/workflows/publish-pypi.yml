name: Publish to PyPI

on:
  release:
    types: [published]
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    
    - name: Extract version
      id: version
      run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
      shell: bash
    
    - name: Install build tools
      run: |
        python -m pip install --upgrade maturin setuptools wheel
    
    - name: Build wheel with maturin (Linux)
      if: runner.os == 'Linux'
      run: |
        maturin build --release --out wheels
    
    - name: Build wheel with maturin (Windows)
      if: runner.os == 'Windows'
      run: |
        maturin build --release --out wheels
    
    - name: Build wheel with maturin (macOS)
      if: runner.os == 'macOS'
      run: |
        maturin build --release --out wheels
    
    - name: Fix wheel metadata
      env:
        VERSION: ${{ steps.version.outputs.version }}
      shell: python
      run: |
        import zipfile, os, tempfile, shutil, sys
        
        whl_files = [f for f in os.listdir('wheels') if f.endswith('.whl')]
        if not whl_files:
            print("ERROR: No wheel file found")
            sys.exit(1)
        
        whl_path = os.path.join('wheels', whl_files[0])
        
        # Read README for the body
        with open('README.md', 'r', encoding='utf-8') as f:
            readme_content = f.read()
        
        # Get version from environment (with v prefix stripped)
        version = os.environ.get('VERSION', '').lstrip('v')
        
        temp_dir = tempfile.mkdtemp()
        try:
            with zipfile.ZipFile(whl_path, 'r') as z:
                z.extractall(temp_dir)
            
            for root, dirs, files in os.walk(temp_dir):
                for file in files:
                    if file == 'METADATA':
                        metadata_path = os.path.join(root, file)
                        
                        lines = [
                            "Metadata-Version: 2.1",
                            "Name: kore-fileformat",
                            f"Version: {version}",
                            "Summary: KORE Binary Format - High-performance columnar compression with 5-8x compression ratio",
                            "Home-page: https://github.com/arunkatherashala/Kore",
                            "Author: Sai Arun Kumar Ktherashala",
                            "Author-email: arunkatherashala@gmail.com",
                            "License: Apache 2.0",
                            "Project-URL: Repository, https://github.com/arunkatherashala/Kore",
                            "Project-URL: Issues, https://github.com/arunkatherashala/Kore/issues",
                            "Description-Content-Type: text/markdown",
                            "Keywords: compression,binary-format,performance,serialization",
                            "Requires-Python: >=3.8",
                            "Classifier: Development Status :: 5 - Production/Stable",
                            "Classifier: Intended Audience :: Developers",
                            "Classifier: License :: OSI Approved :: Apache Software License",
                            "Classifier: Programming Language :: Python :: 3",
                            "Classifier: Programming Language :: Python :: 3.8",
                            "Classifier: Programming Language :: Python :: 3.9",
                            "Classifier: Programming Language :: Python :: 3.10",
                            "Classifier: Programming Language :: Python :: 3.11",
                            "Classifier: Programming Language :: Python :: 3.12",
                        ]
                        # Join headers and append README as body with blank line separator
                        proper_metadata = "\n".join(lines) + "\n\n" + readme_content
                        
                        with open(metadata_path, 'w', encoding='utf-8') as f:
                            f.write(proper_metadata)
            
            os.remove(whl_path)
            with zipfile.ZipFile(whl_path, 'w', zipfile.ZIP_DEFLATED) as z:
                for root, dirs, files in os.walk(temp_dir):
                    for file in files:
                        file_path = os.path.join(root, file)
                        arcname = os.path.relpath(file_path, temp_dir)
                        z.write(file_path, arcname)
        finally:
            shutil.rmtree(temp_dir)
    
    - name: Upload wheel artifacts
      uses: actions/upload-artifact@v4
      with:
        name: wheels-${{ matrix.os }}
        path: wheels/
        retention-days: 1
  
  publish:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
    - name: Extract version
      id: version
      run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
      shell: bash
    
    - name: Download all wheel artifacts
      uses: actions/download-artifact@v4
      with:
        path: all-wheels
        pattern: wheels-*
    
    - name: Consolidate wheels
      run: |
        mkdir -p final-wheels
        find all-wheels -name "*.whl" -exec cp {} final-wheels/ \;
    
    - name: Verify wheels
      run: |
        ls -la final-wheels/
        if [ $(ls -1 final-wheels/*.whl 2>/dev/null | wc -l) -eq 0 ]; then
          echo "ERROR: No wheels found"
          exit 1
        fi
    
    
    - name: Publish to PyPI (OIDC/Trusted Publisher)
      uses: pypa/gh-action-pypi-publish@v1.10.0
      with:
        packages-dir: final-wheels/
        skip-existing: true
    
    - name: Publish summary
      run: |
        echo "## PyPI Publishing Status - v1.1.4 Multi-Platform" >> $GITHUB_STEP_SUMMARY
        echo "Version: ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
        echo "Platforms: Linux, Windows, macOS (universal2)" >> $GITHUB_STEP_SUMMARY
        echo "Wheels published:" >> $GITHUB_STEP_SUMMARY
        ls -1 final-wheels/*.whl | sed 's/^/- /' >> $GITHUB_STEP_SUMMARY





