name: Publish to PyPI

on:
  release:
    types: [published]
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  pypi:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    
    - name: Extract version
      id: version
      run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
    
    - name: Install build tools
      run: |
        python -m pip install --upgrade maturin setuptools wheel
    
    - name: Build wheel with maturin
      run: |
        maturin build --release --interpreter python3.12 --out wheels
    
    - name: Fix wheel metadata
      run: |
        python3 << 'PYEOF'
        import zipfile, os, tempfile, shutil, sys
        
        whl_files = [f for f in os.listdir('wheels') if f.endswith('.whl')]
        print(f"Found wheels: {whl_files}")
        if not whl_files:
            print("ERROR: No wheel file found")
            sys.exit(1)
        
        whl_path = os.path.join('wheels', whl_files[0])
        print(f"Processing: {whl_path}")
        
        temp_dir = tempfile.mkdtemp()
        try:
            print(f"Extracting to: {temp_dir}")
            with zipfile.ZipFile(whl_path, 'r') as z:
                z.extractall(temp_dir)
                print(f"Extracted files: {z.namelist()}")
            
            metadata_found = False
            for root, dirs, files in os.walk(temp_dir):
                print(f"Checking dir: {root}")
                for file in files:
                    if file == 'METADATA':
                        metadata_found = True
                        metadata_path = os.path.join(root, file)
                        print(f"Found METADATA: {metadata_path}")
                        
                        proper_metadata = """Metadata-Version: 2.1
Name: kore-fileformat
Version: 1.1.0
Summary: KORE Binary Format - Complete 8-language ecosystem
Home-page: https://github.com/arunkatherashala/Kore
Author: Sai Arun Kumar Ktherashala
Author-email: arunkatherashala@gmail.com
License: Apache 2.0
Classifier: Development Status :: 5 - Production/Stable
Classifier: Intended Audience :: Developers
Classifier: License :: OSI Approved :: Apache Software License
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.8
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
"""
                        
                        with open(metadata_path, 'w') as f:
                            f.write(proper_metadata)
                        print("METADATA replaced successfully")
            
            if not metadata_found:
                print("ERROR: METADATA file not found!")
                sys.exit(1)
            
            print("Removing old wheel...")
            os.remove(whl_path)
            print(f"Creating new wheel: {whl_path}")
            with zipfile.ZipFile(whl_path, 'w', zipfile.ZIP_DEFLATED) as z:
                for root, dirs, files in os.walk(temp_dir):
                    for file in files:
                        file_path = os.path.join(root, file)
                        arcname = os.path.relpath(file_path, temp_dir)
                        z.write(file_path, arcname)
            print("Wheel recreated successfully")
        finally:
            print(f"Cleaning up: {temp_dir}")
            shutil.rmtree(temp_dir)
        PYEOF
    
    - name: Publish to PyPI (OIDC/Trusted Publisher)
      uses: pypa/gh-action-pypi-publish@v1.10.0
      with:
        packages-dir: wheels/
        skip-existing: false

    - name: Publish summary
      run: |
        echo "## PyPI Publishing Status" >> $GITHUB_STEP_SUMMARY
        echo "Version: ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
        echo "Status: Completed" >> $GITHUB_STEP_SUMMARY





